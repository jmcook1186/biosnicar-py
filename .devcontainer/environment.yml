name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - xarray
  - dask
  - netCDF4
  - bottleneck
  - scipy
  - pip
  - pip:
      - miepython
      - click==8
      - statsmodels
      - seaborn
      - plotnine
      - pre-commit
      - flask
      - gunicorn
      - requests
      - flask-cors
      - bump2version
      - streamlit
prefix: /opt/conda
