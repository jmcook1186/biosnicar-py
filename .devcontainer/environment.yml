name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - xarray
  - dask
  - netCDF4
  - bottleneck
  - scipy
  - pip:
      - miepython
      - statsmodels
      - seaborn
prefix: /opt/conda
