name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - pip:
    - python-dotenv
    - streamlit
    - pandas
    - dask[complete]
    - matplotlib
    - miepython
    - scipy
    - seaborn
    - plotnine
    - statsmodels
    - pytest
    - tqdm
    - pyyaml
    - bump2version
    - plotly
prefix: /opt/conda
