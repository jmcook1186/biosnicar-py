name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - pip:
    - streamlit
    - pandas
    - matplotlib
    - miepython
    - netCDF4
    - scipy
    - seaborn
    - xarray
    - pytest
    - tqdm
    - pyyaml
    - plotly
prefix: /opt/conda
