name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - pip:
    - streamlit
    - pandas
    - matplotlib
    - miepython
    - scipy
    - seaborn
    - plotnine
    - statsmodels
    - pytest
    - tqdm
    - pyyaml
    - plotly
prefix: /opt/conda
